#include <stdio.h>

int main() {
    int month;

    // Input month number
    printf("Enter month number (1-12): ");
    scanf("%d", &month);

    // Determine month name and days using switch-case
    switch(month) {
        case 1:
            printf("January, 31 days\
