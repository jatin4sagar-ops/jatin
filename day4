#include <stdio.h>

int main() {
    int a, b;

    // Input two numbers
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    // Swap without third variable (using arithmetic)
    a = a + b;
    b = a - b;
    a = a - b;

    // Output result
