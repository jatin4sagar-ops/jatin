#include <stdio.h>

int main() {
    int n, sum;

    // Input n
    printf("Enter n: ");
    scanf("%d", &n);

    // Formula for sum of first n natural numbers: n*(n+1)/2
    sum = n * (n + 1) / 2;

    // Output result
    printf("Sum=%d\n", sum);

    return 0;
}
