#include <stdio.h>

int main() {
    char ch;

    // Input a character
    printf("Enter a character: ");
    scanf("%c", &ch);

    // Check the type of character
    if (ch >= 'A' && ch <= 'Z') {
        printf("U
